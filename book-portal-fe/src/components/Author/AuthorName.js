import React from "react";

const AuthorName = (props) => {

  const { authorName } = props;

  return (
    // <div >
    //   <span >{authorName}</span>
    // </div>
    <span>{authorName}</span>
  );
};

export default AuthorName;

